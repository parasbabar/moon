{
  "name": "@midnight-verify/api",
  "version": "1.0.0",
  "description": "Midnight Verify — TypeScript API layer connecting the contract to the frontend",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'API layer uses Vite alias resolution in frontend build'",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@midnight-verify/contract": "1.0.0",
    "@midnight-ntwrk/compact-runtime": "0.16.0",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  }
}
